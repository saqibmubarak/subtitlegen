# Core dependencies
torch
openai-whisper
ffmpeg-python # Dependency for whisper audio extraction
# Optional: faster-whisper is recommended for better performance,
# but the standard openai-whisper is used here for simplicity and PyTorch GPU integration.